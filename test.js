let m;
let min = 10000, max = 10000000;
m = Math.random() * (max - min) + min;
m = Math.floor(m);
let step = 2;
while (step <= n) {
    if (step % 2 === 0) 
        console.log(step);
    }
    step++;
{let step = 2;
    while (step <= n) {
        // 3 / 2 = 1
        if (step % 2 === 0) {
            console.log(step);{
        }
        step++;
    }